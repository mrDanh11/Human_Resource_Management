/**
 * LoadingSpinner.tsx - Component loading spinner
 * Hiển thị khi đang fetch data từ API
 * Có thể customize size và color
 */

// Spinner component với animation CSS
// Props để control size (sm, md, lg)
// Có thể hiển thị với overlay cho full page loading
